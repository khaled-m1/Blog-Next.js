import React from 'react'

function ClientSideRoute(export default function RootLayout({
    children,
  }: {
    children: React.ReactNode;
  })) {
  return (
    <div>ClientSideRoute</div>
  )
}

export default ClientSideRoute